# Optional — enables cloud sync + login via Supabase.
# Leave these unset to run the app in local-only mode (data stays in the browser).
# Copy this file to ".env" and fill in your own Supabase project values.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
